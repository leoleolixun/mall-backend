# 2026-07-06：当前后端主线 - 稳定已实现模块并进入订单开发

## 今日目标

今天不继续扩散新模块，先把当前 MVP 后端主线稳定下来，然后开始订单模块。

当前主线文档：

- [current-backend-plan.md](/Users/leo/GoWorkSpace/mall/mall-backend/docs/current-backend-plan.md)
- [openapi.yaml](/Users/leo/GoWorkSpace/mall/mall-backend/docs/openapi.yaml)

## 今日学习重点

- GORM 事务
- Repository 如何支持事务
- Service 层业务一致性
- 远程 MySQL 下的启动安全
- OpenAPI 作为接口契约，而不是实现清单

## 今日已完成

- 认证注册和微信首次登录改为事务创建 `users` + `user_auths`
- 地址默认地址相关操作改为事务
- 启动时 `AutoMigrate` 和种子数据改为配置开关控制
- 新增当前后端计划文档
- OpenAPI 扩展为完整系统接口蓝图

## 今日继续完成

### 第 1 步：确认配置

远程 MySQL 建议：

```yaml
app:
  auto_migrate: false
  seed_data: false
```

本地空库开发才打开：

```yaml
app:
  auto_migrate: true
  seed_data: true
```

### 第 2 步：自测已实现接口

按顺序测试：

```text
GET  /health
POST /api/v1/auth/register
POST /api/v1/auth/login/password
GET  /api/v1/me
GET  /api/v1/categories
GET  /api/v1/products
POST /api/v1/addresses
GET  /api/v1/addresses
POST /api/v1/cart/items
GET  /api/v1/cart/items
```

重点确认：

- 登录后能拿到 `access_token`
- 需要登录的接口都带 `Authorization: Bearer <token>`
- 地址默认地址只有一个
- 购物车能正确显示商品、SKU、数量、小计

### 第 3 步：开始订单模型

优先创建：

```text
internal/model/order.go
internal/model/order_item.go
internal/dto/order.go
```

第一版订单状态：

```text
1 待支付
2 已支付
5 已取消
```

订单必须保存：

- 用户 ID
- 商户 ID
- 订单号
- 订单状态
- 金额字段
- 地址快照
- 商品快照

### 第 4 步：不要做的事

今天不要实现：

- 收藏
- 优惠券
- 真实微信支付
- 物流
- 售后
- 商家后台
- 平台后台

这些已经在 OpenAPI 里作为规划接口存在，但不是当前开发主线。

## 今日验收

- [ ] `go test ./...` 通过
- [ ] `go vet ./...` 通过
- [ ] `git diff --check` 通过
- [ ] Swagger 能打开
- [ ] 已实现接口能按 OpenAPI 调试
- [ ] 订单模型和 DTO 已开始

## 今日记录

今天学会的 Go 知识：

-

今天完成的接口或模块：

-

今天遇到的问题：

-

明天优先处理：

-
