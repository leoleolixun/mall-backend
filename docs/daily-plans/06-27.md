# 2026-06-27：第 1 天 - Go 基础、项目骨架、MySQL、Redis

## 今日目标

完成 Go 后端项目最小可运行骨架，并接通 MySQL 和 Redis。

今天不追求业务功能，重点是把后续开发的基础工程打稳。

详细教程见：[06-27-tutorial.md](/Users/leo/GoWorkSpace/mall/mall-backend/docs/daily-plans/06-27-tutorial.md)

## 今日学习

### Go 基础

- `go mod init` 和 Go module 的作用
- package 的组织方式
- `struct`、method、pointer 的基本用法
- Go 显式错误处理习惯
- `defer` 的基础用法

### Web 基础

- Gin 路由注册
- Handler 函数签名
- JSON 响应
- HTTP 状态码

### 工程基础

- 配置文件读取
- MySQL 连接初始化
- Redis 连接初始化
- 日志初始化
- Docker Compose 基础

## 今日完成

### 项目结构

- 初始化 `go.mod`
- 创建基础目录：

```text
cmd/server
internal/config
internal/bootstrap
internal/router
internal/handler
internal/service
internal/repository
internal/model
internal/middleware
internal/dto
pkg/response
pkg/logger
pkg/errors
```

### 启动入口

- 创建 `cmd/server/main.go`
- 加载配置
- 初始化日志
- 初始化 MySQL
- 初始化 Redis
- 注册路由
- 启动 HTTP 服务

### 基础接口

- 实现：

```http
GET /health
```

返回示例：

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "status": "ok"
  }
}
```

### 基础工程

- 创建 `config.yaml`
- 创建 `config.example.yaml`
- 配置远程 MySQL
- 配置本地 Redis
- 定义统一响应结构
- 定义统一错误结构
- 创建 `.gitignore`，避免提交本地真实配置

## 今日验收

- [x] `go run ./cmd/server` 可以启动
- [x] `GET /health` 返回正常
- [x] 服务启动时能连接 MySQL
- [x] 服务启动时能连接 Redis
- [x] 统一响应结构已经可用
- [x] `go test ./...` 编译检查通过
- [x] 真实 `config.yaml` 已加入 `.gitignore`
- [ ] `docker compose up -d mysql redis` 可以启动依赖

说明：今天使用远程 MySQL 和本地已启动 Redis，因此 Docker Compose 依赖启动暂时跳过。

实际健康检查返回：

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "mysql": "ok",
    "redis": "ok",
    "status": "ok"
  }
}
```

## 今日不做

- 不做商品接口
- 不做用户登录
- 不做 JWT
- 不做 Swagger
- 不做订单

## 今日记录

今天学会的 Go 知识：

- Go module 的初始化和 `go.mod` 的作用
- package 名需要和目录职责保持一致
- 配置通过结构体承载，并通过 `Load` 函数返回 `(*Config, error)`
- Go 里错误需要显式返回和处理
- HTTP 响应可以封装成统一 `Response`
- `context.Context` 可以给 Redis Ping 等 IO 操作设置超时
- GORM 初始化后可以通过底层 `sql.DB` 做连接池配置和 Ping

今天完成的接口：

- `GET /health`

今天遇到的问题：

- `go mod tidy` 在没有 Go package 时会提示 `"all" matched no packages`，这是 warning，不是失败
- `config.yaml` 应放在 `mall-backend` 根目录，和 `go.mod` 同级
- 真实数据库和 Redis 密码不能提交，因此改为提交 `config.example.yaml`，本地 `config.yaml` 加入 `.gitignore`

明天优先处理：

- 建立商品相关数据模型：`merchants`、`categories`、`products`、`product_skus`
- 初始化默认商户和测试商品数据
- 实现商品分类、商品列表、商品详情接口
- 给商品详情增加 Redis 缓存
